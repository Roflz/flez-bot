# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local / user-specific (do not ignore submodule dirs; they are tracked)
*.local
*.log

# Bundled installers (run installer_deps\download-installers.ps1 to fetch)
installer_deps/*.exe

# Build output (run bundle-setup.ps1 or pyinstaller / iscc)
build/
dist/
flez-bot.exe
