# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local / user-specific (do not ignore submodule dirs; they are tracked)
*.local
*.log
.env
.env.local
flez_bot_auth_debug.log
__pycache__/
*.pyc

# Bundled installers (run installer_deps\download-installers.ps1 to fetch)
installer_deps/*.exe
installer_deps/*.source-url.txt

# Runtime (bundled Python, etc.)
runtime/python/

# Build output (run bundle-setup.ps1 or pyinstaller / iscc)
build/
dist/
flez-bot.exe
